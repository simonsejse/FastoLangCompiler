fun char writeChar(char x) = write(x)

fun [char] writeCharArray([char] x) = map(writeChar, x)

fun bool isCharB(char x) = 
    (x == 'b')

fun [char] main() = 
    let y = {'a', 'b', 'c'} in
    let z = filter(isCharB, y) in
    writeCharArray(z)